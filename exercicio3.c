#include <stdio.h>
#include <stdlib.h>

int main() {

    printf("Hello world!\n");

    return 0;
}

/* 3) Marque V para verdadeiro e F para Falso nas afirmações abaixo:

(F) A passagem por valor em C altera o valor original do argumento.
(V) Uma matriz em C é uma coleção de elementos do mesmo tipo de dado.
(F) Em C, a passagem por referência é usada para criar cópias dos argumentos originais.
(V) A principal diferença entre os operadores -> e . está na forma como você acessa os membros
   da estrutura: -> é usado com ponteiros para estruturas, enquanto . é usado com variáveis da 
   estrutura.
(V) Em C, é possível sobrecarregar funções, ou seja, ter várias funções com o mesmo nome, mas 
   com diferentes argumentos.
(V) As funções em C podem ter um número limitado de parâmetros.
(F) Funções em C podem ser aninhadas, ou seja, uma função pode ser definida dentro de outra função.
(V) O protótipo de uma função em C deve ser declarado antes de sua primeira chamada.
(V) É possível realizar chamada de uma função diretamente para outra função em C.
(V) Ponteiros em C podem apontar para funções.
(F) Um ponteiro em C não pode ser usado para acessar elementos de um array.
(F) O operador "&" em C é usado para obter o valor apontado por um ponteiro.
(F) Um ponteiro pode ser inicializado com o valor de outra variável em C. 

*/